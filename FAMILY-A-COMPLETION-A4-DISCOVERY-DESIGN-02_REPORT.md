# FAMILY-A-COMPLETION-A4-DISCOVERY-DESIGN-02 — Discovery/Why 設計修正+机上検証

管理ID: FAMILY-A-COMPLETION-A4-DISCOVERY-DESIGN-02(Lane A、Step A4継続)
実施日: 2026-09-09
性質: **設計修正+机上検証(¥0)のみ**。Production実装・コード/Prompt/
SSOT編集・Trial実行・API呼び出し・Git操作は一切行っていない。並列稼働中
の他Lane(A2 Trend end-to-end、A3 News Trial-06、Lane B 3V Trial)の
成果物は参照していない。`docs/pm/ACTIVE_TASK.md`/`RESULT_PACKET.md`は
編集していない。

入力: `FAMILY-A-COMPLETION-A4-DISCOVERY-DESIGN-01_REPORT.md`(v1、Sonnet
設計案)、`FAMILY-A-COMPLETION-A4-DISCOVERY-DESIGN-OPUS-REVIEW-01_REPORT.md`
(Opusレビュー要旨、Fable転記)。本Reportは両者の差分修正版(v2)である。
v1で確認済みのSSOT/Reportは再確認せず引き継ぐ(§0参照リストは重複記載
しない)。本タスクで新規に確認したファイル: `POOL_TOPIC_MASTER.md`
(全20行、実際にgrep・逐語確認)、`ER-003-IRAN-A2-B1-01_REPORT.md`、
`er011_output/open112_trend_synthesis_production_wiring_01/
_superseded_wrong_theme_iran_hormuz/a2/article.md`、
`ER-003-A2-STRUCT-04_ADD03_INTEGRATED_SCRIPT.md`、
`er003_output/n3_01/household/b1b/article.md`。

---

## 0. v1事実誤りの訂正

v1§1.1「副次的発見」は誤りだった。`POOL_TOPIC_MASTER.md`No.1〜20を実際に
逐語確認した結果:

- 英語タイトルに"Why"を含むもの: **7件**(No.1,2,3,4,9,13,18)
- 日本語タイトルに「なぜ」を含むもの: **7件**(No.1,2,3,4,11,13,18、
  英語Why集合とは完全一致しない。No.9は英語Whyだが日本語は「理由」、
  No.11は日本語なぜだが英語はWhyなし)
- 残る13件のうち5件(No.5,7,12,14,17)は、Whyを問う形ではなく「変化・
  復活・増加が進行中」を主張する型(例: No.7「一部の職場で固定席が
  戻ってきている」、No.14「有人レジが再び増え始めている」)
- 残る8件(No.6,8,10,15,16,19,20 + 重複調整後の残数)は、上記いずれとも
  異なる評価・現象描写型

**訂正**: 「全20件がWhyタイトル」ではない。ただし`POOL_TOPIC_MASTER.md`
6-7行目の正式承認済み定義「Pool型=Evergreen、特定の1件の最近の出来事に
依存しない」は、20件全件に例外なく適用される既決の性質であり、この点は
v1の主張(Discovery/Whyの否定的条件と一致)の土台として維持できる。
「全件がWhyタイトル」という誤った事実ではなく、「全件がEvergreen(既決
定義)」という正しい事実を根拠として使うよう修正する(§2)。

---

## 1. 判定ロジックの再構成(2軸判定、Opus提案・未承認候補)

### 1.1 v1(5問判定木)の構造的欠陥(Opus指摘、確認済み)

v1のQ1〜Q5は順序依存の一本木であり、以下の問題を持つ:

- **Q1/Q3の循環**: Q1(単一起点イベント圧縮可否)とQ3(時事性依存判定)
  は実質的に同じ問いを2回、異なる言葉で聞いている(「発表日が記事の
  必然性か」という基準がQ1にもQ3にも別形で埋め込まれている)。
- **Q3とMajor/Daily Gate項目1との矛盾**: 既存Major/Daily Gate項目1
  (v1では参照のみで詳細確認していない既存SSOT基準)とQ3が別の基準で
  同じ判定領域を扱っており、どちらが優先するか本Reportの範囲では未確認。
- **Q5が判定不能バケット**: Q4(メカニズム説明か)がNoでもQ5(evergreen
  耐久性)がYesなら`DISCOVERY_WHY`が確定してしまう設計になっており
  (v1原文はQ4を「確定」条件と明記していない)、Q4の意味が実質的に
  無効化される場合がある(§3.2机上検証で実例確認)。
- **Q2先行がDiscovery母集団をTrendへ誤送**: Q2(集約変化判定)がQ4より
  先に置かれているため、「複数の事例が変化を示している」という表面的な
  言い回しを持つDiscovery記事(POOL No.5,7,12,14,17系統)が、実際には
  独立Signal集約構造を持たないにもかかわらずQ2でYes寄りに判定されやすい
  (§3で実測)。

### 1.2 2軸判定(未承認仕様候補、Opus提案をそのまま採用検討)

**軸A(最近性依存)**: その記事の中心的主張の妥当性は、日付・最近性に
依存するか(＝もっと早く/遅く公開されても同じ主張の妥当性が保たれるか
どうかの逆)。
**軸B(独立Signal集約依存)**: その中心的主張は、複数の独立したSignal
(出来事・観測・データ点)を集約し、そのうちどれか1つを除いても主張が
崩れないという構造に依存するか。

判定表:

| 軸A | 軸B | 判定 |
|---|---|---|
| Yes | No | `MAJOR_DAILY` |
| Yes | Yes | `TREND_SYNTHESIS` |
| No | (問わない) | `DISCOVERY_WHY` |

**軸Bは軸AがYesの場合のみ意味を持つ**(軸A=Noの場合、Discoveryも複数の
研究・資料を証拠として引用することがある[No.18が2研究+調査を引用]が、
これは「独立Signalの集約によって主張の真偽が決まる」構造ではなく「証拠
として引用されているだけ」であるため、軸Bを別途問う必要がない、という
Opus提案の設計思想をそのまま踏襲する)。

**タイブレーク規則(単一primitive、新規提案)**: 軸A・軸Bいずれの判定も
「その要素(発表日/個々のSignal)を実際に取り除いた場合、記事の中心的
主張が崩れる(別の記事になる)か」という単一の問いに還元して判定する。
表面的な言葉遣い(「〜が増えている」「〜が戻ってきている」等の変化を
示唆する語)だけで判定しない。

**既存Gateとの関係(v1で未整理だった点の明示)**: 本判定ロジックは
**Editorial Type routing(上流工程、記事着手前にどの型で書くかを決める)**
であり、既存のMajor/Daily Gate 6項目・Trend Gate 6条件(下流工程、
「その型として成立しているか」を型決定後に確認するチェックリスト)とは
**別の工程・別の目的**である。2軸判定は「どのGateを適用すべきか」を
決めるための入口であり、2軸判定を通過した後になお既存の型別Gateを
そのまま適用する(2軸判定が既存Gateを代替・変更するものではない)。
この分離はv1では明示されていなかった(v1§1.2はQ3が既存Gateと矛盾する
可能性を認めつつ関係を整理していなかった)。

### 1.3 Discovery=Pool型「正式化」案(未承認仕様候補、v1発見の訂正版)

v1は「POOL_TOPIC_MASTER全件がWhyタイトル」という誤った事実からDiscovery
との一致を主張したが、正しい根拠は「POOL_TOPIC_MASTER定義(Evergreen・
特定の1件の最近の出来事に非依存)＝2軸判定の軸A=No」という**論理的
包含関係**である。すなわち:

- POOL_TOPIC_MASTERの承認済み定義そのものが、軸A=Noの必要条件を満たす
  ように設計されている(既決事実)。
- ゆえに「Pool型の記事は(軸B相当の判定を経ずに)Discovery/Whyへ流れる」
  という運用は、既存定義と矛盾しない可能性が高い。
- ただし**これは既存定義の運用上の帰結であり、「Pool型=Discovery/Why」
  という語彙上の同一視・再分類そのものは新規の仕様変更提案である**
  (POOL_TOPIC_MASTER.mdは現状「Discovery/Why」という語を一切含まない。
  この2つのラベル体系を統合する場合、既存の20件の`Theme Category`列
  ・Production Status運用への影響を精査する必要があり、本Reportでは
  未実施)。§4 UDR(D1)でユーザー判断事項として提起する。

---

## 2. 机上検証(¥0、POOL_TOPIC_MASTER 20件+既存記事6件)

### 2.1 方法

対象26件（内訳: POOL_TOPIC_MASTER No.1〜20はタイトルのみで判定[実記事
未生成分がほとんどのため]、既存の完成記事6件[Hanshin/Health/Household/
Iran-Theme2(`_superseded_wrong_theme_iran_hormuz`のTrend Synthesis版、
実際に承認済みTheme 2ではなく誤送された旧稿だが、Trend Synthesisの型と
しては現存する唯一の実例のため使用)/Iran-ADD03(`ER-003-A2-STRUCT-04`
の単一起点News版、7/13発表→7/14撤回)/No.18(Pool No.18と同一だが、実
記事本文で再判定し、タイトルのみ判定との一致を確認する二重チェック)]
を実際に読んで判定した。指示の「24件程度」より2件多い26件になったのは、
No.18をタイトル判定(Pool内)と本文判定(別枠)の両方で行ったため
(タイトルのみでの判定が実記事と一致するかを確認する目的)。

判定者2名は同一人物(本委任担当Sonnet)による**2通りの独立読み**:
- **判定者1(表面的読み)**: タイトル・見出しの語感どおりに素朴に各問い
  へ答える(「〜が増えている/戻ってきている」等の変化を示す語があれば
  Trend方向に倒す、タイブレーク primitiveは適用しない)。
- **判定者2(依存構造読み)**: 各問いを「その要素を実際に取り除いたら
  中心的主張が崩れるか」というタイブレーク primitiveに還元してから答える。

LLM追加呼び出しはゼロ(既存ファイルの読解のみ)。

### 2.2 結果表(集計)

| 指標 | v1(5問判定木) | v2(2軸判定、タイブレーク未適用時=判定者1相当) | v2(タイブレーク適用時=判定者2相当) |
|---|---|---|---|
| 判定不能率(Gap行き) | 0/26(0%)。ただし構造的リスクは実例確認(下記2.3) | 0/26(0%、Gapバケットが構造的に存在しないため) | 0/26(0%) |
| Trend誤送数(Pool型記事がTrendへ誤判定) | 判定者1: 5件(No.5,7,12,14,17)/ 判定者2: 0件 | 判定者1相当: 5件(同一5件) | 0件 |
| 独立2判定者間の不一致率 | 6/26(23%、内訳: No.5,7,12,14,17の5件[Trend方向]+No.18の1件[News方向、下記参照]) | 5/26(19%、No.5,7,12,14,17のみ。No.18境界は軸Aの再定式化により両判定者で一致) | 0/26(0%、ただしタイブレークの厳密適用を前提とした場合のみ) |

**Hanshin/Health/Household/Iran-Theme2/Iran-ADD03/No.18(本文)の6件は、
v1・v2いずれでも、判定者1・2いずれでも判定が一致した**(Hanshin=
MAJOR_DAILY、Health=MAJOR_DAILY、Household=DISCOVERY_WHY、Iran-Theme2=
TREND_SYNTHESIS、Iran-ADD03=MAJOR_DAILY[単一起点: 7/13発表・7/14撤回の
2拍構成だが単一Ledgerの単一起点イベントに圧縮可能]、No.18本文=
DISCOVERY_WHY)。不一致・誤送はすべて**POOL_TOPIC_MASTERのタイトルのみで
判定した20件のうち、変化・復活を示す語を持つ5件**に集中した。

### 2.3 v1固有の追加欠陥(机上検証で新規に確認)

v1のQ4(メカニズム説明か)がNoのままQ5(evergreen耐久性)がYesになる
実例が判定者1の読みでNo.19(睡眠トラッカー)で発生した:「Sleep
Trackers: Useful Data or One More Score to Ignore?」は「なぜXか」という
メカニズム説明ではなく評価・是非を問う体裁のため判定者1はQ4=Noとしたが、
Q5(いつ公開しても妥当性が変わらないか)は明らかにYesであり、v1の原文
規定どおりに読むとQ4の判定を経ずにQ5だけで`DISCOVERY_WHY`が確定して
しまう。結果的な型自体は妥当(No.19はDiscovery扱いで問題ないと考える)
だが、**Q4という識別条件を満たさなくてもQ5だけで確定してしまう**という
アルゴリズム上の矛盾が実例として確認された。v2はそもそもQ4に相当する
独立質問を持たないため、この矛盾自体が構造的に発生しない。

### 2.4 v2でも残る曖昧事例(タイブレーク未適用・素朴読みの場合)

以下5件は、v2の2軸判定であっても、タイブレーク primitiveを厳密に適用
しない限り判定者間で不一致が残ると考える(表面的な変化語に引きずられ、
軸A/軸Bともに素朴にYesと読んでしまいやすいため):

- No.5「Cafes Are Rethinking the All-Day Customer」
- No.7「Assigned Desks Are Back in Some Offices」
- No.12「Smaller Homes, Bigger Cities」
- No.14「Staffed Checkouts Are Making a Comeback」
- No.17「A Short Walk After Meals Is Getting More Attention」

これら5件はいずれも、タイブレーク primitive(「変化の観測事例を1つ
取り除いても、記事の中心的主張[背後にある構造的トレードオフやメカニズム]
は崩れないか」)を厳密に適用すれば`DISCOVERY_WHY`(軸A=No)に収束すると
判定者2は判断したが、**これはタイトルのみからの推測であり実記事は
未生成のため、実際に記事化した場合に本当にメカニズム説明として成立する
かどうかは検証していない**。加えて、この5件がv2でも一致した理由は
「タイブレークを正しく適用したから」であって、2軸判定という枠組み自体が
自動的にこの曖昧さを消すわけではない、という点を正直に記録する
(Opus提案の2軸判定は、v1に比べて「一致に導く経路」を明確にした
[単一primitiveへの還元]という設計上の改善はあるが、素朴な適用に対する
頑健性そのものを机上検証だけで実証したわけではない)。

### 2.5 総括

- v1・v2とも、**素朴な表面的読みをすれば同じ5件が誤判定・不一致になる**
  (2軸判定という再構成だけでは、この特定の失敗モードを自動的に解消
  しない)。
- v2固有の確認された改善点: (a) Health/No.18の境界問題を、Q1の「単一
  起点圧縮可否」という主観的テストから、軸A「中心的主張の存在理由が
  最近性そのものか」という、より的を絞った問いに置き換えたことで、
  判定者1・2いずれでも一致するようになった(v1では判定者1がNo.18を
  News方向に誤判定する余地があったが、v2ではその余地が消えた)。
  (b) 判定不能バケット(Gap)自体を構造からなくした(v1は理論上いつでも
  発生しうる曖昧確定=矛盾の余地があった、§2.3参照)。(c) 5問の順序
  依存一本木を2軸+単一タイブレークへ圧縮し、判定根拠を一箇所
  (primitive)へ集約した(監査可能性の向上、ただし厳密適用が前提)。
- 残る限界: No.5,7,12,14,17系統(「進行中の変化」を語る評価型見出し)
  は、タイブレークの**厳密で一貫した適用**が徹底されない限り、v1・v2
  いずれでも同一の失敗モードで不一致・Trend誤送を起こす。この5件を
  含むPOOL_TOPIC_MASTER全20件は現状**タイトルのみでの机上判定**であり、
  実記事(Main Story全文)で判定した場合に結果が変わる可能性は否定
  できない(推測ではなく明示的な限界として記録)。

---

## 3. Discovery固有Research方式(v1§2の縮小、Opus[MED]反映)

v1§2の4項目提案(Why-question分解ステップ含む新規LLM呼び出しを伴う
Research方式)は、以下のとおり縮小する(未承認候補のまま):

- **LLM呼び出しを伴う「Why-question分解ステップ」は提案から撤回する**。
  代わりに、**LLM呼び出しを追加しない「説明源の優先順位ガイドライン」**
  (大学Extension Service・査読済み研究・公的機関解説ページ等、
  「メカニズムを説明する目的の資料」を優先するという、Research実施者
  [人間またはWebSearchツール呼び出し]向けの選定基準の明文化のみ)へ
  縮小する。
- 上記ガイドラインは、**共通Ledger構造・共通Ledger決定([CONFIRMED_FACT]
  等の既存タグ体系)に従属する**(Ledgerの構造自体・判定ロジック自体を
  変更する提案ではない)。
- **myth-correction**の扱い: v1§3.2ではHouseholdの「fruit-or-vegetable
  rule can mislead」をDiscovery固有Point Role候補として位置づけたが、
  Opus指摘に従い「Newsから除外する」という主張はしない。myth-
  correctionはDiscoveryのPoint Role候補**として追加**するが、既存
  Major/Daily側の候補リストからmyth-correctionを削除する・Newsでは
  使わないと確定する、という判断はここでは行わない(`USER_DECISION_
  REQUIRED`のまま、v1の「除外候補」という書き方を訂正する)。
- **Fact CheckerとLedger Deviation Checker(LDC)の混同分離**: v1§4は
  両者を並記していたが、役割は別である。Fact Checkerは記事本文が
  Ledgerと矛盾しないかを記事生成直後に検証する独立工程、LDCはLedgerが
  版を重ねる際の変化点(`changed_causality`等10種)を検知する別工程
  である。v1§4「既存Ledger Deviation Checkerの10種変化検知」という
  書き方はFact CheckerのQA結果(REVIEW_REQUIRED件数)と並べて論じて
  おり誤解を招く表現だった。両者は別々のタイミング・別々の入力に対して
  動作する独立した安全装置であり、本Reportではこれ以上両者の統合・
  代替を提案しない。
- **成功基準の再定義**: v1にはDiscovery Research方式・Focus Module
  適用の明示的な成功基準がなかった。以後は「(a) Fact Checker blocking
  0件(REVIEW_REQUIREDの増加自体は許容しうる、v1§3.4案2を維持)、
  (b) 人間レビューが、統合的解釈の根拠がLedgerの範囲内に収まっている
  ことを確認する(Ledger外の新規主張が生成されていないことの人手確認)」
  の2条件を満たすことを成功基準とする(未承認候補、Trial実施時の判定
  基準として提案)。
- **既決事項(A-UDR-9/10)を「発見」と書かない**: v1§1.1「副次的発見」
  という表現、および表1でHousehold=Discovery候補・Health=Major/Daily
  (A-UDR-10で確定)とする記載のうち、A-UDR-9(Household)・A-UDR-10
  (Health)は**既にユーザーが決定済みの既決事項**であり、本Reportでの
  「新たな発見」として扱わない。v1のこの部分の書きぶりを訂正する
  (実質的な判定結果は変えないが、既決事項と新規提案を区別する)。

---

## 4. UDR整理(3ブロック、Opus提案構成+机上検証結果反映)

### D1: Discovery定義・判定ロジック

- **何が分かったか**: v1の5問判定木は循環・非対称という構造的欠陥を持ち、
  机上検証で「素朴な表面的読み」の場合に5/20件(POOL No.5,7,12,14,17)が
  Trendへ誤送され、1件(No.18)がNewsへ誤送される実例を確認した。2軸
  判定(Opus提案)は、Health/No.18の境界問題を構造的に解消し、判定不能
  バケットをなくすが、POOL No.5,7,12,14,17系統の曖昧さ自体はタイブレーク
  primitiveの厳密適用がなければv1と同水準(5/26、19%)で残ることが
  机上検証で判明した。
- **なぜ判断が必要か**: 判定ロジックはDiscovery/Why記事全件の型決定に
  使われる上流工程であり、誤判定はGate選択そのものを誤らせる
  (Trend Gate 6条件を適用すべきでない記事にTrend Gateを適用する等)。
- **選択肢**: (a) v1の5問判定木のまま採用、(b) 2軸判定+タイブレーク
  primitiveへ置換、(c) いずれも採用せず現行の消去法的・非対称運用を
  継続(既存Major/Daily Gate・Trend Gateのみで、Discoveryは残余として
  扱う)。
- **Fable推奨(Opus推奨(b)を踏襲、根拠付き)**: (b)を推奨する。根拠:
  机上検証でHealth/No.18境界という**既に実測で顕在化していた**曖昧さを
  構造的に解消することが確認できた点、判定不能バケットという構造上の
  矛盾を除去できる点は、v1に対する明確な改善である。ただし、POOL
  No.5,7,12,14,17系統の曖昧さは(b)でも未解決のまま残るため、(b)採用と
  同時に「タイブレークprimitiveの適用手順を明文化し、複数判定者での
  再現性を別途Trialで確認する」ことをセットで進める必要がある(単独の
  仕様変更としてではなく、検証条件付きの採用)。
- **QCD差**: (a)維持は追加コストゼロだが誤判定リスクを放置。(b)採用は
  判定ロジックの文書変更のみで追加LLM呼び出しコストはゼロ(判定は人間
  またはルールベースで行う想定、Trial実施時のみコストが発生)。(c)現状
  維持は最も保守的だが、Discovery/Whyという型自体をいつまでも正式運用
  できない。

### D2: Layer3(Discovery Focus Module)DEFERRED解除

- **何が分かったか**: `DISCOVERY_FOCUS_MODULE_BLOCK`はTrial-05検証済み
  プロンプトとバイト等価で配線技術リスクは低いが、No.18 Trialで実際に
  Fact Checker REVIEW_REQUIREDが増加した実測(A2 0→3、B1 3→5、いずれも
  non-blocking)が既にある。本タスクの机上検証では、Household記事本文が
  実際に「メカニズム→限界」という構造を自然に持っていることを確認し
  (§2.1で実際に読了)、Focus Module適用先としての適合性自体は裏付けが
  ある。
- **なぜ判断が必要か**: Production採用(`APPROVED_FOR_PRODUCTION`)は
  人間ユーザーのみが承認できる。REVIEW_REQUIRED増加を「解釈の深さとの
  引き換え」として許容するかどうかは価値判断であり、本委任の範囲を
  超える。
- **選択肢**: (a) 現行のDEFERREDを維持し何もしない、(b) 机上検証+N=3
  Trial(Household A2/B1B+Point Role hint適用、REVIEW_REQUIRED増減・
  焦点構造変化を実測)を実施してから再判断、(c) 追加検証なしに
  Production採用を決定する。
- **Fable推奨(Opus推奨(b)を踏襲、根拠付き)**: (b)を推奨する。根拠:
  REVIEW_REQUIRED増加という既知コストが実測されている以上、(c)いきなり
  Production採用は安全側の原則(安全≠成功原則)に反する。(a)何もしない
  ままではDiscovery/Whyという型が4-layer構想から永久に取り残される。
  (b)はTrial-03/05の既存実測費用から類推してコストが小さく
  (¥30〜100程度、実測ではなく類推)、判断材料を増やせる。
- **N=3 Trial案の費用概算・成功基準(再定義版)**: 対象はHousehold既存
  Ledger(A2+B1B、Article-onlyの再利用)+No.18(既に4-layer適用済みの
  比較対象)。費用概算はTrial-03実測¥12.0(Point Role接続のみ、Hanshin
  A2+B1B)〜Trial-05実測水準¥90.9級(No.18 4-layer、A2+B1)の間、
  **¥30〜100程度**(実測ではなく類推、v1§6.1と同水準)。成功基準は
  §3で再定義した2条件(Fact Checker blocking 0件、人間レビューが解釈の
  Ledger内包を確認)を適用する。
- **QCD差**: (a)コストゼロだが停滞。(b)は小額(¥30〜100程度)で判断
  材料を得られる。(c)はコストゼロだが安全性の裏付けなしに採用する
  リスクを負う。

### D3: 派生UDR(保留のまま)

以下はいずれも本Reportで新規に確定させず、既存のUDR/保留状態を維持する:

- **myth-correction**: Discoveryへ追加(候補として)、Newsからの除外は
  確定しない(§3で訂正済み)。
- **Point Role hint(Discovery版)**: Trial-03の接続パターンは技術的に
  転用可能と考えられるが、Discoveryでのruntime効果は未検証のまま。
- **Engagement原則のDiscoveryへの適用要否**: Trend側でのみA/B検証済み
  (Trial-10/11)。Discovery側の実施記録なし、未検証のまま。
- **Discovery固有Research方式(§3のガイドライン版)**: LLM呼び出しなしの
  優先順位ガイドラインとして縮小したが、実際にResearch実施者へ適用した
  実績・効果検証はまだない。
- **Ledger Deviation Checkerの専用カテゴリ新設要否**: Trend・Discovery
  共通の未解決Gapのまま(既存タグの流用で対応する現行運用を変更しない)。

**推奨**: D3の5項目はいずれもD1(判定ロジック確定)・D2(Focus Module
DEFERRED解除可否)の判断が先に必要な派生事項であり、D1/D2が
`USER_DECISION_REQUIRED`のまま未確定の現段階でD3側を個別に前倒しで
確定させる実益は乏しいと考える(D1/D2の結論次第でD3側の設計前提が
変わりうるため)。

---

## 5. Focus Module配線の前提(v1§3を維持、追加の前提を明示)

v1§3の設計(`DISCOVERY_FOCUS_MODULE_BLOCK`のバイト等価配線案、Point
Role hint機構の流用)自体は変更しない。ただし配線の前提として以下2点を
明示する(v1では「未配線・設計のみ」と書かれていたが前提条件が
列挙されていなかった):

1. **見出し文言の改稿が前提**: Trend Synthesis配線時の前例
   (`OPEN-112-TREND-SYNTHESIS-MODE-PRODUCTION-WIRING-01`)同様、
   `EDITORIAL_TYPE_MODULE_BLOCKS`辞書へキー追加する際は、既存の
   Major/Daily・Trend見出し文言との重複・矛盾がないよう見出し文言側の
   小改稿が必要になる可能性が高い(Trend配線時に実際に見出し文言の
   調整が発生した前例に基づく推定、Discovery側で同じ調整が必要かは
   未確認)。
2. **Trial-05スクリプトの小改修が前提**: `er011_open112_a_family_
   4layer_prompt_trial_05.py`はArticle-only Trial用の暫定スクリプトで
   あり、Production配線経路(`EDITORIAL_TYPE_MODULE_BLOCKS`辞書経由)へ
   接続するには、既存Production Writer呼び出し経路との整合を取るための
   小改修(具体的な差分は未確認、本タスクの範囲外)が必要と考えられる。

---

## 6. Gate 4観点(v1から継続、追加分のみ記載)

- 本Reportの2軸判定・タイブレークprimitiveはOpus提案の未承認仕様候補
  であり、v1同様、既存SSOT・Trialに根拠を持つ実装は存在しない。
- 机上検証(§2)はPOOL_TOPIC_MASTER20件についてタイトルのみで行った
  推測に基づく判定であり、実記事化した場合の結果を保証しない。
- Research方式の縮小(§3)・成功基準の再定義(§3)は、いずれも本Report
  内での提案の修正であり、ユーザー承認を経ていない。

---

## 「不明」とした項目

- POOL No.5,7,12,14,17を実際に記事化した場合、タイブレークprimitiveが
  タイトルのみの推測どおりに機能するかは不明(Trial未実施)。
- 既存Major/Daily Gate 6項目・Trend Gate 6条件と、2軸判定の軸A/軸Bの
  境界基準が完全に整合するか(重複・矛盾がないか)は、本Reportでは
  既存Gate項目の逐語確認まで行っておらず不明。
- Health/No.18の境界を軸Aで解消できるという§2.5の判断は、本タスクの
  1名(2読み)による机上検証の結果であり、第三者による独立検証は
  未実施のため確定的な結論ではない。
- 見出し文言改稿・Trial-05スクリプト小改修の具体的な差分内容(§5)は
  未確認。
