# ER-003-P2A: B2文長計測ロジック修正仕様書

## 1. 問題

`er003_output/p2/` に保存済みのB2記事について、文長計測ロジック(`split_sentences`)が
実際には存在しない長文("59語の文"、"37語の文")を計測結果として報告していた。
B2本文そのものではなく、計測ロジックにバグがあった。

## 2. 根本原因

修正前の `split_sentences()` は、文分割を行う前に全ての空白文字(段落間の空行を含む)を
単一スペースに置換していた(`re.sub(r"\s+", " ", ...)`)。さらに文境界を検出する正規表現
`_SENTENCE_SPLIT_RE` は、ピリオド等の直後にストレートクォート(`"` `'`)しか許容せず、
生成テキストで多用されているカーリークォート(`'` `'` `"` `"`)を認識できなかった。

その結果、`bedtime."` のように「ピリオド→閉じカーリークォート→空白」という並びでは
文末とみなされず、段落をまたいで次の段落(場合によってはさらに次の段落)と連結されてしまい、
実際には存在しない長文が生成されていた。

- A02の「59語」: 3つの独立した段落が誤って1文に連結されたもの。
- ADD03の「37語」: 見出し直後の短い行と、コロン導入の引用ブロックが誤って連結されたもの。

## 3. 修正内容

`er003_ja_to_en_translation.py` の文分割ロジックを段落認識型に全面書き換え:

1. `split_into_paragraphs()`: 空行で段落分割を先に行う(段落境界を越えて連結しない)。
2. `split_paragraph_into_sentences()`: 各段落の内部でのみ文分割を行う。
   - 終端記号(`.!?`)の直後に、ストレート/カーリー問わず閉じ引用符・閉じ括弧が
     続く場合も正しく文末として認識する(`_TRAILING_CLOSERS`)。
   - 終端記号の後、次の非空白文字が大文字・数字・開き引用符であれば新しい文の開始と
     判定し(`looks_like_new_sentence`)、小文字であれば継続とみなす。これにより
     `a.m.`/`p.m.` のような時刻表現は文脈に応じて正しく判定される。
   - `U.S.`/`Mr.`/`St.` 等、常に同一文内で固有名詞が続く敬称・国名略語のみを
     単語境界チェック付きで無条件保護する(`_is_protected_abbreviation`)。
   - 小数点(`_is_decimal_point`)は数字に挟まれたピリオドとして保護する。
3. 終端記号を持たない段落(見出し直後の単独行等)は、それ自体を1つの独立した単位として扱う。

`compute_word_count()` の単語数定義は変更していない(変更理由が見当たらなかったため)。

## 4. 影響範囲

- `split_sentences()` を呼び出す全ての箇所(P1の `compute_sentence_metrics`、B2アダプタの
  `compute_b2_sentence_metrics` 等)が、この修正の恩恵を自動的に受ける。
- 既存の生成済みB2本文(`b2_version_raw.md`)は一切変更していない(sha256で検証済み)。
- API呼び出しは0件。
