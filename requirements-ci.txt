# requirements-ci.txt
#
# Pythonバージョン: 3.12 (py -3.12 で作成した .venv-ci で検証)
# 作成目的: AUTO-001のローカルCIテスト基盤(scripts/run_ci_tests.py)を
#   再現可能な環境で実行するための、CI専用の最小依存関係を固定する。
# 生成・更新方法:
#   1. `py -3.12 -m venv .venv-ci` でクリーンな仮想環境を作成する
#   2. `.venv-ci` を有効化し、以下5つの直接依存だけをインストールする
#        numpy / scipy / openai / python-dotenv / google-genai
#   3. `python scripts/run_ci_tests.py` を実行し、全テストが成功することを確認する
#   4. 成功した環境で `python -m pip list --format=freeze` を実行し、
#      その出力全体(直接依存5件+推移依存)でこのファイルを置き換える
#   5. 別のクリーンな .venv-ci を作り直し、
#      `python -m pip install -r requirements-ci.txt` だけで
#      同じテスト結果を再現できることを確認してから更新を確定する
# 最終検証日: 2026-07-27 (AUTO-001-03B, Windows 11 / win32)
#
# 注意: このファイルは本番コード(記事生成・音声合成等)や実験スクリプト
#   (generate_test.py, tts_test*.py 等)全体の依存関係を表すものではない。
#   ci_test_manifest.json の"include"に登録されたCIテストファイルの
#   importと実行に必要な依存関係だけを対象とする。

annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
colorama==0.4.6
cryptography==49.0.0
distro==1.9.0
google-auth==2.56.2
google-genai==2.14.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jiter==0.16.0
numpy==2.5.1
openai==2.48.0
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
requests==2.34.2
scipy==1.18.0
sniffio==1.3.1
tenacity==9.1.4
tqdm==4.69.1
typing_extensions==4.16.0
typing-inspection==0.4.2
urllib3==2.7.0
websockets==16.1.1
