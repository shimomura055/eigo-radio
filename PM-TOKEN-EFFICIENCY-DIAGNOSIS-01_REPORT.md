# PM-TOKEN-EFFICIENCY-DIAGNOSIS-01 診断報告書

管理ID: `PM-TOKEN-EFFICIENCY-DIAGNOSIS-01`(読み取り専用診断、Sonnet委任、¥0。
コード・Prompt・SSOT・`docs/pm/ACTIVE_TASK.md`/`RESULT_PACKET.md`・並列稼働中
タスクの生成物への編集は一切行っていない)。対象: Fable以外(Sonnet/Opus
subagent)のToken消費削減余地の診断。モデルルーティング方針・Opus起用方針
そのものの変更提案ではなく、運用改善候補の提示にとどめる。

---

## エグゼクティブサマリー

- Fable提供実測(2026-09-10、15委任、subagent合計≈2.32M token/日)のうち、
  **有効な消費**(実装・Trial実行・Opus設計レビュー・精密SSOT編集)が大半
  (概算約85〜90%、≈2.0M token/日)を占め、モデルルーティング方針自体を
  変更する根拠は見当たらない(Opus 4件は毎回具体的な欠陥・矛盾を検出し
  手戻りを未然回避)。
- **重複・削減可能な消費**(即時実施可、Fable運用変更のみで完結)は
  概算**125,000〜195,000 token/日**(全体の約5.4〜8.4%)。内訳: (a)Git記録
  専用委任の最小化 ≈40,000〜50,000/日、(b)読取専用REPORT編纂とSSOT反映
  Agentの統合 ≈60,000〜100,000/日、(c)Opus/回帰実行のスコープ事前指定
  ≈25,000〜45,000/日。
- 加えて、**記録様式(SSOT行の肥大化)がユーザー判断を要する形で追加
  50,000〜100,000 token/日**の削減余地を持つ(`OPEN_ITEMS.md`の1行が
  最大61,963文字[実測28,855 token]まで肥大化しており、些細な1行修正
  [`OPEN-135記述矛盾の是正`、diffはわずか1行増減]に88,871 tokenを要した
  実例あり)。
- 合計削減見込み: 即時実施可のみで**約5〜8%/日**、記録様式変更を含めれば
  **最大約10〜12%/日**(いずれも「見込み」であり、Fable側の正確なツール
  呼び出しログは未参照のため概算)。

---

## 1. Fableとsubagentの重複

- `docs/pm/PM_BRIEF.md`(126行)・`PM_GOVERNANCE.md`(1,167行、該当節1/2-1/
  8/11/12のみで約340行)・Agent定義3件(`sonnet-worker.md`44行/
  `opus-consultant.md`27行/`sandwich-pm.md`65行)を確認した限り、Agent定義
  自体は簡潔で「毎回全文必読」の指示は持たない(Fable側の手順4節で
  「必要なSSOT箇所だけをGrep」と明記)。**Agent定義自体は大きな重複源では
  ない。**
- 重複源は構造的に別にある。Fableは委任前にGrepで「確認済み事実」を得るが
  (`sandwich-pm.md`手順2)、Production安全性(`sonnet-worker.md`「新しい
  仕様候補を勝手に実装しない」)を担保するため、Sonnetは独立に同じSSOT
  箇所を再確認する設計になっている。これは意図的な安全設計であり、
  単純な「無駄」ではない。
- 実例(具体箇所): `OPEN-135記述矛盾の是正`(commit `9ab655b`、diffは
  `OPEN_ITEMS.md`1行のみ、+1/-1)は、Sonnetが`prompt.txt`(A2/B1B)を再grep
  し事実確認したうえで訂正しており、これ自体は正しい安全手順である。ただし
  この確認のために`OPEN_ITEMS.md`のOPEN-135行(40,242文字≈実測換算約
  18,700 token)を複数回(確認前・訂正後の検証)読み込む必要があり、
  88,871 tokenのうち相当部分がこの1行の反復読み込みに費やされたと推定
  される(タスク自体の正味の作業は1行diffのみ)。
- **削減案(即時実施可、仕様変更なし)**: Fableが委任文に「確認済み事実」を
  埋め込む際、対象を「管理ID名」ではなく「ファイル名+行番号(または
  一意な検索文字列)」まで具体化する。例: 「`OPEN_ITEMS.md`のOPEN-135行
  (5)の"cautionary_constrained"記述を確認」のように、Sonnetの再確認Grepが
  該当行のピンポイント一致になるよう誘導する(現状でも概ねそうなっている
  が、巨大行では"該当行"自体が数千〜数万文字あるため、行を特定しても
  読み込みコストは下がらない。真の解決は3節の記録様式改善)。
- **見込み削減**: Agent定義側の縮小では削減効果は小さい(数百〜1千token/
  委任程度)。SSOT行特定の精密化のみでは巨大行問題を解決しないため、本節
  単独の効果は限定的(数千token/委任)。真の効果は3節(記録様式)と合わせて
  発揮される。

---

## 2. Opusレビューの縮小可能性

`docs/pm/MODEL_ROUTING_TRIAL_LOG.md`のTrial導入後実績表から、過去4件の
Opus L2レビュー全件を確認した。

| 管理ID | token | 秒 | 検出内容(具体的指摘) |
|---|---|---|---|
| `EDITORIAL-B-FAMILY-VOICES-3V-4V-WRITER-DESIGN-OPUS-REVIEW-01` | 108k | 379 | Fable委任案の事実誤り2件(Writer経路不在/Ledger作成方式誤り)、Production欠陥候補1件(`build_voice_attribution_block()`のevidence欠落率91.1%[行]/87.3%[文字]を実測で確認)、fail-open再発点2件(segment名ハードコードによる新segment名の安全機構すり抜け)、未承認仕様候補4件 |
| `FAMILY-A-COMPLETION-A4-DISCOVERY-DESIGN-OPUS-REVIEW-01` | 121k | 327 | 判定木の循環(Q1/Q3)・Gate項目との矛盾(Q3)・判定不能バケット(Q5)・Trend誤送(Q2先行)、およびPOOL_TOPIC_MASTER件数の事実誤り(全20件Why誤り、実際は英語Why7件・日本語なぜ7件) |
| `FAMILY-A-POINT-QUALITY-RECONCILIATION-OPUS-REVIEW-01` | 100k | 372 | Sonnet横断整理の因果連鎖の誤り2件(「もぐらたたきが主因」/「Focus ModuleがRole収束を引き起こす」の主張)を実データ・コードで反証、実装欠陥候補1件、交絡候補1件。**後日の段階1事後再集計で支持または条件付き支持と確認済み**(誤検出ではなかった) |
| `FAMILY-A-NEWS-STAGE4-REDESIGN-INVENTORY-01-OPUS-L2-REVIEW` | 不明(Report非作成) | 不明 | 候補案の根拠が閾値未再校正のartifactと指摘、真の論点(Evidence Allocation)を提起し、後続の¥0監査Triggerとなった |

- 4件とも「全論点レビュー」ではなく、Fableが委任前に「なぜHIGHか/何を
  確認したいか/Sonnet単独で何が不足か」を明示した**論点限定型**の依頼
  だった(`MODEL_ROUTING_TRIAL_LOG.md`各行の「選定理由」列で確認)。
  にもかかわらず、いずれも当初想定外の欠陥・矛盾・因果誤りを検出しており、
  「論点を絞ったのに拾えた」という点で費用対効果は良好と評価できる。
- **重要な観察**: 3件目(`POINT-QUALITY-RECONCILIATION`)が指摘した「因果
  連鎖の誤り」は、4件目(`NEWS-STAGE4-REDESIGN-INVENTORY`)の選定理由でも
  「過去に同種のSonnet整理で因果関係の誤りが複数回見つかった実績」として
  再度Opus起用の根拠にされている。**これはOpus起用が場当たり的でなく、
  実績に基づく再現性のある判断基準になっていることを示す**(儀式的起用の
  兆候はない)。
- **縮小可能性の評価**: 100k〜121kのうち、Fableが渡す論点自体は既に絞られて
  いる。Opusはtool(Read/Grep/Glob)で自らコード・SSOT・関連Reportを横断
  探索しており、token消費の大半はOpus自身の探索コストである。委任文で
  「確認すべきファイル・行番号のリスト」を事前に絞り込めば(例:
  「`OPEN_ITEMS.md`のOPEN-131/132行、`er009_diagnostic_full_retry_
  modules_12.py`の該当関数のみ確認」)、Opusの自律探索範囲を狭められ、
  見込み15〜20%程度(1件あたり15k〜25k token)の削減余地はある。
- **リスク**: 4件のOpusレビューはいずれも「Fableが事前に想定していなかった
  論点」(例: POOL_TOPIC_MASTERの件数誤り、evidence欠落率の実測)を検出
  している。ファイルリストを過度に狭めると、Opusが独自に発見する経路
  (横断grepでの偶発的発見)を塞ぐ可能性がある。**「論点限定+diff/表のみ
  渡す」形式への完全移行は推奨しない**(過去の指摘4件中、少なくとも2件
  [POOL_TOPIC_MASTER件数誤り、evidence欠落率実測]はFableが論点として
  明示していなかった副次的発見であり、範囲を絞りすぎると同種の発見を
  失うリスクがある)。**「主要確認先を提示しつつ、Opus自身の追加探索は
  妨げない」中間案**(見込み削減10〜15%、≈10k〜18k token/件)を推奨する。

---

## 3. contextの絞り込み

`OPEN_ITEMS.md`(表向き365行)・`DECISION_LOG.md`(10,668行)・
`CURRENT_SPEC.md`(1,136行)の実際の行長を機械集計した。

| ファイル | 総行数 | 最大行文字数 | 実測token(Read実測) | 該当項目 |
|---|---|---|---|---|
| `OPEN_ITEMS.md` | 365 | 61,963文字 | **28,855 token**(1行のみ読込で実測) | OPEN-120(B Family Voices Trial状態) |
| `OPEN_ITEMS.md` | 365 | 56,999文字 | 概算約26,500 token | OPEN-112 |
| `OPEN_ITEMS.md` | 365 | 40,242文字 | 概算約18,700 token | OPEN-135(Family A Completion Program) |
| `OPEN_ITEMS.md` | 365 | 25,631文字 | 概算約11,900 token | OPEN-121 |
| `DECISION_LOG.md` | 10,668 | 36,062文字(1行のみ、他は平均120文字/行) | 概算約16,800 token | 該当エントリ(表形式部分) |
| `CURRENT_SPEC.md` | 1,136 | 28,217文字 | 概算約13,100 token | CONSOLIDATION-08関連記述 |

**問題の実体**: `OPEN_ITEMS.md`は「1 Open Item = 1テーブル行」の様式であり、
新しい進捗・訂正が発生するたびに**同じ行へ追記**する運用になっている。
その結果、OPEN-120・OPEN-112・OPEN-135のような継続案件の行が数万文字
まで肥大化した。Grep/Readで「OPEN-135だけを見る」つもりでも、マッチした
1行がまるごと返るため、絞り込みの効果がなくなっている(2節の
`OPEN-135記述矛盾の是正`タスクが好例: 1行diffの修正に88,871 token)。
`DECISION_LOG.md`・`CURRENT_SPEC.md`にも同型の巨大単一行が部分的に存在する
(件数はOPEN_ITEMS.mdほど多くない)。

- **削減案A(即時実施可、Fable運用のみ)**: 巨大行が既知の管理ID
  (OPEN-112/120/121/135等)については、Fableが委任文で「該当行全体では
  なく、直近の追記部分[例: 末尾から500文字]または特定の小見出し[例:
  "(5)"]のみを確認せよ」と明示する。ただしGrep/Readはツールの性質上、
  1行マッチだと丸ごと返るため、この案単独の効果は限定的(該当行を
  そもそも読まざるを得ない場面では効果なし)。
- **削減案B(ユーザー判断要、記録様式の改善であり仕様変更ではない)**:
  肥大化した行を「サマリ行+詳細別ファイル」または「日付ごとの追記行を
  複数行に分割」する様式へ移行する。例: `OPEN-135`のような継続案件は
  `OPEN_ITEMS.md`本体には現在Statusの数行サマリのみを残し、経緯の全文は
  既存の`ER-*_REPORT.md`または`DECISION_LOG.md`エントリへのリンクで
  代替する(すでに`DECISION_LOG.md`に同内容が重複記載されている箇所も
  複数確認できる)。この場合、OPEN-135/120/112/121の4行合計で概算
  75,900文字[実測換算約35,000〜40,000 token]の読込コストが、サマリ行
  (概算各500〜1,000文字)へ置き換わることで、**該当4行のみで1回の読込
  あたり約30,000〜35,000 token削減**。これらの行は本日確認できただけで
  複数タスクから繰り返し参照されており(2節・5節参照)、**1日あたりの
  累積削減見込みは50,000〜100,000 token**。
- **リスク・実装コスト**: 既存の巨大行を分割する一時的な移行作業
  (Sonnet委任1回、概算100k〜150k token程度)が必要。移行中に情報欠落
  (経緯の言い換えによる意味変化)のリスクがあるため、移行は「切り貼りの
  みで要約しない」ことを条件にすべき。

---

## 4. モデル呼び出し不要化

- **具体例1(実測、最も確実な削減余地)**: commit `7bc125a`
  (`PM-CLOSEOUT-CONSOLIDATION-65`、Fable記録上「分析REPORT 3件のGit記録」
  27,918 token・25秒)は、`git show --stat`で確認したところ**新規作成
  ファイル3件の追加のみ**(`git add`3ファイル→commit→push、差分は
  `1,052 insertions(+)`のみで削除・変更行なし)。同様にcommit `3056fa7`
  (Fable記録上「ルーティングREPORTのGit記録」28,571 token)も新規ファイル
  1件の追加のみだった。**この種の「新規ファイルのGit記録のみ」タスクは、
  判断要素(commit message文面はFableが既に確定している)がなく、
  Sonnetへのフル委任(PM_BRIEF/ACTIVE_TASK読込・システムプロンプト等の
  固定オーバーヘッドを含む)は過剰**。
  - 削減案: (a)即時実施可(ユーザー判断不要): Fableの委任文を「これら
    Nファイルを`git add`→指定のcommit message→push→`git status`で
    確認、のみ実施せよ。SSOT/ACTIVE_TASK/PM_GOVERNANCEの読込は不要」と
    明示し、Sonnetの探索範囲を機械的に縮小する。見込み削減: 28k→
    8k〜12k程度(60〜70%減、1件あたり約16k〜20k)。
    (b)ユーザー判断要: sandwich-pm(Fable)自身に、ごく限定的な
    `git add`(ファイル名指定)/`commit`/`push`のみを許可する専用の
    軽量ツール権限を与える(Bashではなく、拡張子・パスをホワイトリスト
    制限した固定スクリプト実行に限定)。これは`sandwich-pm.md`の
    「自分ではしないこと」の変更(Agent定義変更)にあたるため、
    ユーザー判断が必要。実現すれば0 token・¥0化できる。
- **具体例2**: 回帰テストは`run_project_regression.py --pattern`で
  ファイル範囲を絞り込める(既存の正規スクリプト、`unittest discover`
  直接呼び出しではないため8節の既存ルールに抵触しない)。現在の実装
  タスクでは変更module関連のみ`--pattern`指定で反復実行し、Production
  wiring前の最終1回のみdefaultパターン(102ファイル、`er0*_test_*.py`
  全件)で実行する運用にすれば、実装中の反復回帰実行コストを削減できる。
  **これは既存ツールの機能内であり仕様変更ではない**(即時実施可)。
  見込み削減: 実装セッション中の反復回数に依存するため一律見積り不可だが、
  1回の全件実行あたりの出力・ログ読込コストを想定すると、反復2〜3回を
  scoped実行に置き換えることで1タスクあたり5,000〜15,000 token程度の
  節約が見込める。
- **具体例3**: `docs/pm/MODEL_ROUTING_TRIAL_LOG.md`の実績表への追記
  (「統合(closeout consolidation)タスク実行時に、Sonnetが本ファイルの
  表へ該当管理IDの行を追記する」という現行ルール)は、Fableが既に
  保有する実測値(token・秒・差し戻し回数等)をSonnetが単純転記するのみの
  作業であり、機械的なテンプレート生成(スクリプトまたはHaiku定型
  artifact)で代替可能。見込み削減: 該当作業が単独タスクの場合で
  5,000〜10,000 token程度(通常は他のSSOT反映作業と合算実施されているため
  単独の影響は小さい)。
- **具体例4(Trial結果JSONからのREPORT表自動生成)**: 今回確認した範囲では
  Trial系REPORT(News Trial-12等)の「費用(段階別実測)」表は、既存の
  cost計測ログ(`cl.install()`等の既存費用記録機構)から数値を人力転記して
  いるように見える。数値転記部分をscript化すれば数千token程度の削減余地
  があるが、今回はscript化の実装コストとの比較材料(既存ログの構造)を
  精査しておらず、見込みは「不明、要追加調査」とする。

---

## 5. 並列subagentの独立性

- **具体的な重複読み込みの実例**: `FAMILY-A-NEWS-STAGE4-STATUS-REPORT-01_
  REPORT.md`(読取専用編纂、149,944 token)の「参照した主要ファイル」節には
  以下が明記されている。
  - `FAMILY-A-NEWS-STAGE4-REDESIGN-INVENTORY-01_REPORT.md`(443行)
  - `FAMILY-A-NEWS-STAGE4-EVIDENCE-ALLOCATION-AUDIT-01_REPORT.md`(215行)
  - `FAMILY-A-NEWS-STAGE3-NEW-THEME-LEDGER-TRIAL-09_REPORT.md`
  - `FAMILY-A-NEWS-STAGE2-DIAGNOSTIC-BRANCH-TRIAL-08_REPORT.md`
  - `OPEN_ITEMS.md`(OPEN-133/134/135/136/137/138/139/140行、複数の巨大行を
    含む)
  - `DECISION_LOG.md`(6エントリ)

  この後、`PM-CLOSEOUT-CONSOLIDATION-65`(234,311 token)がこの
  `STATUS-REPORT-01`を含む分析REPORT3件をSSOT(`DECISION_LOG.md`/
  `OPEN_ITEMS.md`)へ転記する際、**同じ内容を別Agentが再度読み直して
  要約・転記している**。これは意図的な役割分離(読取専用の独立レビューが
  編集Agentの認知バイアスを避ける)の一種の効果はあるが、今回のケースは
  「レビュー」ではなく「同一著者路線の編纂→転記」であり、独立性による
  品質向上効果は限定的と評価できる。
- **概算重複コスト**: `STATUS-REPORT-01`(149,944 token)自体が既に
  4本のReport全文+SSOT該当行を読み込んだ結果であり、後続の
  `CONSOLIDATION-65`(234,311 token、News Stage4状況報告・Discovery仕様
  reconcile分析・3V Production wiring計画の3件を統合)はこれらを
  **再読**したうえでSSOTへ転記している。統合タスクの234,311 tokenの
  うち、3件のReport(合計501+139+412=1,052行)の再読込に要したと推定
  される分は、概算で60,000〜100,000 token程度(3件のReport分量・複雑な
  内容を踏まえた概算。正確な内訳はFable側のツール呼び出しログが必要)。
- **統合案(即時実施可)**: 「読取専用REPORT編纂」と「SSOT反映」が
  同一日・同一Lane内で連続する場合(今回のNews Stage4/Discovery
  reconcile/3V wiring plan→CONSOLIDATION-65のように)、両者を1回の
  Sonnet委任内で連続実施する(編纂した直後にそのままSSOT反映まで
  行わせる)。これにより後続Agentの再読込コストが不要になる。
  見込み削減: 60,000〜100,000 token/日(上記概算)。
- **分離が有効だった例(逆の事例)**: 4節のOpus L2レビュー4件はいずれも
  「Sonnetの整理・実装を、別モデル・別視点のAgentが読み直す」ことで
  価値を生んでいる(2節参照。特に`POINT-QUALITY-RECONCILIATION-OPUS-
  REVIEW-01`はSonnetの因果推論の誤りを検出し、後日の再集計で支持
  されている)。**「モデルが変わる」「視点が変わる(実装者→監査者)」
  独立レビューは維持すべきで、統合すべきは同モデル・同性質(読取編纂→
  転記のみ)の連続作業に限定する。**

---

## 6. 進行中案件の具体調整案

| 案件 | 現状(実測) | 調整案 | 削減見込み | 品質リスク |
|---|---|---|---|---|
| News Trial-12(Ledger拡充、`FAMILY-A-NEWS-STAGE4-LEDGER-ENRICHMENT-AB-TRIAL-12_REPORT.md`) | Ledger拡充調査(web_search 12回、gpt-5.6-sol、reasoning=high)が**入力110,837 token・出力8,494 token(うち reasoning 6,000)で¥148.6**(Trial予算¥200の74%)、記事生成4本は¥36.0のみ | (a)検索回数の上限設定(例: 8回まで)、(b)reasoning effortを`high`→`medium`へ試験的に引き下げ、(c)候補Xとして既に案内されている通りLedger再利用(2回目以降は¥148.6が発生しない) | (c)は既にReportで提示済み・追加費用¥0化(候補X採用で以後¥148.6/テーマが不要に)。(a)(b)は未検証だが、検索本文が入力tokenの大半を占めるため、検索回数半減で入力token・費用も概ね半減(¥70〜75程度)の見込み | (a)(b)はFact Safety(事実精度)に直結するため、Ledger作成はQA・Validatorの検証プロセスの一部にあたる可能性があり、**PM_GOVERNANCE 11節STOP必須条件2「QA・Validator・閾値の変更」に該当しうる。ユーザー判断要**。(c)Ledger再利用は既にReport候補として提示済みで低リスク |
| Discovery N増し(`FAMILY-A-DISCOVERY-GENERALIZATION-TOWELS-TRIAL-11_REPORT.md`、¥117.72/テーマ・198k token) | N=1、Ledger作成+記事2本 | 既存Ledgerの再利用(News Trial-12の候補Xと同型の考え方)、および複数テーマを1回のSonnetセッション内でバッチ実行し委任オーバーヘッド(PM_BRIEF/ACTIVE_TASK読込等の固定費)を1回に集約 | 固定費部分(委任あたり概算10k〜20k程度)をテーマ数で按分できるため、テーマ数3件をバッチ化した場合で合計20k〜40k程度の削減見込み | 低(既存承認範囲内の題材差し替えTrialであり、11節「低コスト分析・Trialの自律実施」の対象内と評価できる) |
| 3V Phase 1〜3(296k token、実装+36 test+regression) | `run_project_regression.py`のdefaultパターンで102ファイル全件を都度実行している可能性 | `--pattern`引数で変更module関連ファイルのみに絞った反復実行→Production wiring前の最終1回のみ全件実行(4節具体例2、既存ツール機能内） | 実装セッション中の反復回数に依存、1タスクあたり5,000〜15,000 token程度(4節と同一見積り) | 低(スコープ縮小は最終closeout前に必ず全件へ戻すため、8節「回帰実行ルール」の趣旨[`unittest discover`直接使用の禁止]には抵触しない。ただし「反復中は全件を見ていない」ことのFableへの明示が必要) |
| 2V・3V新テーマ記事全般 | REPORT定型部分(エグゼクティブサマリー/費用表/参照ファイル節等)を毎回Sonnetが手動構成 | REPORT冒頭の定型節(管理ID・費用表・参照ファイル一覧)をテンプレート化し、既存の費用ログ・ファイルパスから半機械的に生成する軽量スクリプトを用意 | 数千token/件程度(小さいが積み重ねで日次1万token規模) | 低(定型部分のみ対象、本文の分析・判断部分は従来通りSonnetが作成) |

---

## 7. 利用量連動の節約モード

**設計案**: 週間利用量(またはコスト)が事前に定めた閾値(例: 通常週の
150%等、具体的な数値はユーザー判断)に達した場合、Fableが以下の
「節約モード」へ切り替える。

- Git記録専用委任は4節の軽量委任形式(SSOT/ACTIVE_TASK読込省略)を必須化
- 読取専用REPORT編纂は要点箇条書き+数値表のみとし、長文の解釈・示唆
  記述を最小化(5節の統合も同時に必須化)
- consolidation(SSOT反映+Git)は、報告可能な単位が複数貯まっても
  **1日1回へ集約**
- 並列Agent起動上限を2へ制限
- Opus起用は2節「中間案」(主要確認先を事前提示)を必須化

**副作用・矛盾点**:

- 「consolidationを1日1回へ集約」は、2026-09-10にユーザーが正式決定した
  `docs/pm/PM_GOVERNANCE.md`12節「報告単位管理ルール」12-2(「1つの報告
  単位が報告可能になったら即時報告する」「他の並列作業の完了を待たずに
  報告する」)と**直接矛盾する**。12節は同日付での正式恒久ルールであり、
  高利用量時であっても即時報告原則を緩めてよいという例外規定は現時点の
  SSOTには存在しない。
- 12-3/12-4(未回答報告のフル再掲義務)と「節約モード」下での報告簡略化も
  緊張関係にある。未回答項目が積み上がった状態で報告を間引くと、
  かえって次回のフル再掲対象が増え、長期的には token消費が増える可能性
  がある。
- **判定(推奨)**: 「Git記録の軽量化」「REPORT編纂とSSOT反映の統合」
  「Opus中間案」は利用量に関わらず常時適用してよい低リスク・低副作用の
  改善であり、**利用量連動にする意味は薄い**(常時実施すべき)。一方
  「consolidationの1日1回集約」「並列上限2への制限」は12節と明確に
  矛盾するため、**利用量連動モードとして新設する場合は12節の例外条件と
  して明示的にユーザーが承認する必要がある**(Fable/Sonnetが自律的に
  「利用量が高いので今日はまとめて報告する」と判断することは、11節
  STOP必須条件5[複数の妥当な選択肢がありユーザー価値判断が必要]に該当
  すると考えられる)。

---

## 運用改善候補一覧

### 即時実施可(Fable運用のみで完結、仕様変更なし)

| 案 | 削減見込み | 品質リスク | 実装コスト |
|---|---|---|---|
| Git記録専用委任の軽量化(SSOT/ACTIVE_TASK読込省略を委任文で明示) | 1件あたり約16k〜20k(60〜70%減)、2件/日で概算40k〜50k/日 | 低(対象を新規ファイル追加のみのcommitに限定すれば誤爆リスク小) | 低(委任文の書き方変更のみ) |
| 読取専用REPORT編纂とSSOT反映の同一セッション内連続実施(同モデル・同性質作業に限定) | 60,000〜100,000/日(5節概算) | 低〜中(「独立レビューとしての価値」がある組み合わせ[Opus等]には適用しない) | 低(委任設計の変更のみ) |
| Opus L2レビューの「主要確認先事前提示+追加探索は妨げない」中間案 | 1件あたり10k〜18k(10〜15%減) | 中(範囲を狭めすぎると副次的発見[POOL_TOPIC_MASTER件数誤り等]を逃すリスク。「主要確認先の提示」に留め「限定」にしないことが必須) | 低 |
| 回帰実行の`--pattern`スコープ活用(既存スクリプト機能内、最終1回は全件) | 1タスクあたり5,000〜15,000 | 低(closeout前に全件へ戻すことを条件化) | 低 |
| REPORT定型節(費用表・参照ファイル一覧)の半機械化 | 数千/件(積算で1万/日規模) | 低 | 中(軽量スクリプト整備が必要) |
| Discovery/News Ledgerの既存Ledger再利用(候補X型の運用を標準化) | テーマあたり¥148.6相当(News)/¥117.72相当(Discovery)の追加調査費用がゼロ化 | 低(Reportで既に提示済みの承認範囲内Trial) | 低 |

### ユーザー判断要(Agent定義変更・記録様式変更等)

| 案 | 削減見込み | 品質リスク | 実装コスト |
|---|---|---|---|
| `OPEN_ITEMS.md`等の巨大単一行を「サマリ行+詳細は既存Report/DECISION_LOGへのリンク」形式へ分割 | 50,000〜100,000/日(継続的、3節参照) | 中(移行時の情報欠落リスク。「要約せず切り貼りのみ」を条件化すれば低減可) | 中(初回移行作業に概算100k〜150k token、Sonnet1タスク相当) |
| Fable(sandwich-pm)へ限定的なGit操作権限(ファイル名指定add/commit/push専用の固定スクリプト)を付与 | Git記録専用委任を0 token・¥0化(1件あたり最大28k) | 低〜中(誤操作防止のホワイトリスト設計が前提。`sandwich-pm.md`「自分ではしないこと」の明示的変更) | 中(Agent定義・許可ツールの変更) |
| News Trial-12 Ledger研究の検索回数上限・reasoning effort引き下げ | ¥148.6→¥70〜75程度(テーマあたり) | 中〜高(Fact Safety・QA閾値に関わる可能性、11節STOP必須条件2に該当しうる) | 低(パラメータ変更のみだが承認必須) |
| 利用量連動節約モードのうち「consolidation 1日1回集約」「並列上限2」 | 不明(7節参照、報告単位管理ルールとの整合次第) | 中(12節「即時報告」原則と矛盾。例外条件の正式承認が前提) | 低(運用ルールの例外規定追加) |

---

## ユーザー判断が必要な項目(推奨+理由)

1. **`OPEN_ITEMS.md`等の巨大単一行の記録様式改善**(推奨: 実施)。
   理由: 実測で1行28,855 token・1行修正に88,871 tokenを要した具体例が
   あり、投資対効果(初回移行概算100k〜150k token、以後日次50k〜100k
   token削減)が明確。ただし「要約せず切り貼りのみ」を移行条件とし、
   情報欠落を防ぐこと。
2. **Fableへの限定的Git操作権限付与**(推奨: 保留、まず軽量委任形式を
   試す)。理由: 軽量委任形式(即時実施可欄参照)だけでも大部分の削減
   (60〜70%)が見込め、Agent権限変更というより大きな変更を急ぐ必要性は
   低い。軽量委任形式を試したうえで効果不十分なら再検討。
3. **News/Discovery Ledger研究の検索回数上限・reasoning effort調整**
   (推奨: 慎重に検討。Ledger再利用[候補X]の標準化を先に実施し、それでも
   新規テーマ調査コストが問題になる場合のみ、Fact Safetyへの影響を個別に
   評価したうえで判断)。理由: Fact Safetyに直結するQA/Validator相当の
   変更であり、コスト削減を理由に安全性を下げるべきではない。
4. **利用量連動節約モード**(推奨: 「Git軽量化」「REPORT統合」「Opus
   中間案」は利用量に関わらず常時適用、「consolidation集約」「並列上限
   引き下げ」は新設しない)。理由: 後者は2026-09-10正式決定の12節
   「報告単位管理ルール」(即時報告原則)と直接矛盾し、Fable/Sonnetが
   自律的に運用ルールの優先順位を決めるべきではないため。

---

## QCD

- **Quality**: 本診断は読み取り専用・¥0で実施し、コード・Prompt・SSOT・
  他タスクの生成物への影響はない。数値は可能な限り実測(Read/Bashによる
  文字数・行数の直接計測、`git show --stat`によるdiff実測)に基づくが、
  「削減見込み」はいずれも概算であり、Fable自身のツール呼び出しログ
  (今回参照対象外)を用いた精緻化が今後可能。
- **Cost**: 本タスク自体のtoken消費は追って`docs/pm/RESULT_PACKET.md`へ
  記録される(Fable記録用)。API呼び出し・Git操作は一切行っていないため
  追加費用¥0。
- **Delivery**: 全7項目の調査・運用改善候補一覧・ユーザー判断項目を
  1回の委任内で完了。追加の調査(Fable側ツール呼び出しログとの照合等)が
  必要な場合は別途追加委任が必要。
