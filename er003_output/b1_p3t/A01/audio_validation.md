# ER-003-B1-P3T 音声検証レポート(機械確認のみ、自然さ・聞きやすさの判定なし)

## 1. Step 1: 前回(ER-003-B1-P3S)raw音声の確認

対象: `er003_output/b1_p3s/A01/raw/span_03_ja_after.wav`

| 項目 | 結果 |
|---|---|
| 存在 | OK |
| decode | OK |
| duration | 4.251秒 |
| clipping | 検出なし |
| 発音の自然さ(「を」の聞こえ方) | **INCONCLUSIVE**(機械的な判定手段を持たないため) |

このraw音声は、ER-003-B1-P3Sの結合処理(`assemble_audio`はチャンク間に無音を挿入するだけで、チャンク自体の内容は変更しない)でそのまま結合サンプルへ使われている。したがって、結合後に聞こえた「を」の不自然さがあるとすれば、それは結合処理由来ではなく、この生成段階(助詞始まりの断片としての単独生成)に起因する可能性が高いと機械的に推定できる。ただし、実際の発音の自然さそのものは判定していない。

## 2. 日本語通し音声

| 項目 | 結果 |
|---|---|
| 存在・decode | OK |
| duration | 8.451秒 |
| sample rate / channels | 24000Hz / 1(モノラル) |
| clipping | 検出なし |
| `を記録できないまま`の分割 | なし(1回のTTS callで全文生成、分割していない) |
| `を`の存在(TTS入力script内) | あり |

## 3. 差し込み位置の検出

`枠内シュート、`の直後に挿入した読点(句読点調整は1文字のみ)により生成された無音区間を、日本語通し音声内でRMSベースに走査した。

| 項目 | 結果 |
|---|---|
| 検出区間 | 2.95秒〜3.37秒(duration 0.42秒) |
| 分割点(区間の中心) | 3.16秒 |

**曖昧性の確認**: 同じ日本語通し音声内の他の無音候補(自然な語間の間、単語境界等)は、いずれも0.02〜0.16秒に収まっており、検出した0.42秒の区間が突出して長い。次点(4.19〜4.35秒、0.16秒)との差は2.6倍以上あり、誤った位置を検出した可能性は低いと判断した。

## 4. 英語Key Phrase単独音声

| 項目 | 結果 |
|---|---|
| 存在・decode | OK |
| duration | 3.691秒 |
| clipping | 検出なし |

## 5. 結合音声(差し込み後)

| 項目 | 結果 |
|---|---|
| 順序 | 日本語(前) → 英語Key Phrase → 日本語(後) |
| `shot on target`使用回数 | 1回(挿入した英語音声そのもの) |
| 境界無音1(挿入直前、3.16秒地点) | RMS 0.0(完全無音) |
| 境界無音2(挿入直後) | RMS 0.0002(実質無音) |
| raw(Dynamics3適用前)decode | OK、12.542秒 |
| final(Dynamics3適用後)decode | OK、12.542秒 |
| clipping(raw / final) | いずれも検出なし |
| Dynamics3適用 | 適用済み(1回のみ) |
| ラウドネス整合 | target -18.62 LUFS → final -18.62 LUFS approx(誤差0.0 LU、目標0.3 LU以内) |

## 6. duration整合性

日本語通し音声(8.451秒)+ 境界無音2箇所(0.2秒 × 2 = 0.4秒)+ 英語Key Phrase(3.691秒) = 12.542秒。結合サンプルの実測durationと完全一致し、分割・挿入の過程で音声内容が失われていないことを確認した。

## 7. source変更確認

`source_japanese_full_sentence`のsha256は、実行前後で一致(`67e5175a...`で不変)。TTS入力用script(`tts_japanese_script.txt`)は句読点1文字の追加のみで、語彙・語順・助詞の変更はない。

## 8. API呼び出し・再試行

| 対象 | call数 | retry数 |
|---|---:|---:|
| 日本語通し音声 | 1 | 0 |
| 英語Key Phrase | 2 | 1(技術的失敗による1回のみの再試行、同一payload) |

品質目的の再生成は行っていない。

## 9. 対象外(ユーザー判断が必要)

音声の自然さ、「を」の聞こえ方(改善したかどうか)、日英切替の違和感は、本レポートでは判定していません。試聴の上でご判断ください。
