# RESULT_PACKET: USER-TEST-NEWS-2EP-COMPLETION-01-CORRECTION-01

0. T-0: `check_delegation_prompt.py` = FAIL(必須セクション見出し2件「事前指定Grep一覧+追記位置・更新位置の手順」「実行コマンド全覧」の見出し文言不一致のみ、内容自体は充足)。継続。
1. 正式仕様: CURRENT_SPEC.md L1273(Pronunciation Ledger→Secondary ASR Phrase List配線範囲)、L1094(Fact Checker REVIEW_REQUIRED=non-blocking advisory)、L1121/L1218(固有名詞ASR自動PASS条件)、L1237(Human Review Cost Guard)、L1278-1279(Human Review Route・Production call site一覧)。
2. コード実行順序: `er003_v1_crosslevel_audio_02_common.py::generate_english_segment_with_fallback()`が標準route(`generate_narration_snippet_verified_strict`、L273でledger_phrases取得)→fallback route(L126でledger_phrases取得)の順で呼び、いずれも`pronun_ledger.get_hint_for_text(text, min_confidence="low")`→`secondary_asr.evaluate_attempt_with_cascade()`。未解決entityへの`_resolve_unresolved_entity_for_review()`(Perplexity research→Ledger upsert)はCascade全4step失敗**後**にのみ発火する(`er006_secondary_asr_01.py` L211-243/L725-745)。
3. **重要な訂正**: 事前調査対象`full_story_part1_attempt2_custom35d6860b.wav`(standard route,"Troy Mink")は、現行final wav(`.../a2/narration/full_story_part1.wav`)とsha256不一致。現行final wavは**fallback(minimal instruction) attempt3**("Troy Mike")と一致(sha256実測で確認)。`phrase_list_used=false`の直接原因は2点: (a)標準/fallback両routeとも、当該call時点でLedgerに"Meink"が未登録(登録はattempt2失敗直後の`updated_at=19:54:48`、fallback[19:55:56]より前だが後述(b)により無関係)、(b)**`er006_pronunciation_ledger_01.py::get_hint_for_text()`が`entry["surface"] in text`という大文字小文字を区別する部分一致判定であり、Ledger格納surfaceは小文字("meink")、canonical textは"Meink"(大文字M)のため、**Meinkが登録済みの現在でも一致せず空リストを返す**(read-only実測で確認、`ledger_get_hint_for_text_min_confidence_low: []`)。これはコード側の未修正バグであり、本タスクでは修正していない(Production改修禁止のため報告のみ)。
4. Ledger状態: "Meink"エントリは`entity_type=cascade_unresolved_entity`・`confidence=low`・`updated_at=2026-09-16T19:54:48`(attempt2失敗直後の事後登録、両route呼び出し時点では未登録または(3)のcase bugにより実質参照不可)。本タスクでの追加登録・更新は0件(git diffのledger.json変更は本タスク実行前からの未commit分、`updated_at`確認済み)。
5. 是正再検証(read-only Ledger確認+正式cascade関数`er006_secondary_asr_01.evaluate_attempt_with_cascade_detail()`を現行final wavに対し`ledger_phrases=["Meink"]`明示指定で実行、TTS呼び出しなし): Azure Secondary#1/#2とも`phrase_list_used=true`(phrase_list_size=1、"Meink"含む)を確認したが、Azure transcriptは両回とも"Troy **Mike**"のまま不一致継続。`final_status=ASR_VALIDATION_UNCERTAIN`、`verified=false`。証跡: `er014_output/user_test_news_2ep_01/space_weapons/a2/audit/correction_01_secondary_asr_phrase_list.json`。
6. A2 Assembly/Audio Gate: 未実施(ケースB、Aに進めない)。
7. API追加コスト実測(`er014_output/.../a2/audit/correction_01_raw_usage_log.jsonl`): OpenAI ASR(primary_2)1回458 token、Azure STT(secondary_1/2)2回×35.171秒。概算¥3程度(cost logger実測ベース)。Perplexity再クエリ0件(cache hit)。
8. Git: 追加ファイルのみをexplicit addでcommit予定(delegation_log・_check.json・correction_01_*・RESULT_PACKET・ACTIVE_TASK)。ledger.json/attempt_history.jsonlはこのタスク以前からの未commit差分であり、このタスクでは変更・stage共にしていない(diffのタイムスタンプで実証済み)。
9. **現在Status: `USER_DECISION_REQUIRED`(ケースB)**。Phrase List付きSecondary ASRを正式関数で正しく実行しても"Meink"のASR不一致は解消せず。HUMAN_APPROVED記録禁止・TTS再生成禁止のままSTOP。
10. 未決事項: (a)上記(3)のget_hint_for_text大文字小文字バグ(Production未修正、Fable/ユーザー判断待ち)。(b)副次発見: Human Review Queue/`review_lock_state.json`双方とも、`wav_path`は現行final wav(fallback版)と同一パスを指すが、記録されたASR transcript本文は**standard route(attempt2,"Troy Mink")のもの**であり、実際にそのパスに存在する音声(fallback,"Troy Mike")のtranscriptとは一致しない(fallback routeが`review_lock`のguarded_generateでラップされていないため)。安全上の実害(誤PASS等)は無いが、監査証跡の正確性に関する既存実装の隙間として報告(修正はしていない)。(c)B1 Fact Checker: space_weapons B1の`fact_check_attempts.json`実データも`verdict=REVIEW_REQUIRED`で、CURRENT_SPEC.md L1094・DECISION_LOG.md L175(ER-010-NO9-FACTCHECK-POLICY-AND-POINT-COMPRESSION-DIAGNOSTIC-12)の非blocking advisory定義と整合、扱い変更なし。(d)Dangling Reference Check: Pronunciation Ledger/Secondary ASR/Phrase List/Human Review Lockの各Production参照先(コード)はCURRENT_SPECの記述と一致(上記(a)(b)はコード内部バグ・証跡精度の課題であり、SSOT記述と実装参照先の食い違いではない)。
11. 無変更証跡: `git status --porcelain CURRENT_SPEC.md DECISION_LOG.md OPEN_ITEMS.md er006_*.py er003_*.py er011_*.py`は、上記ledger.json以外の対象ファイルについて空(Ledger.jsonのみ、原因は本タスク以前の未commit分)。事前指定外Read: なし(事前指定Grep結果からのみ絞り込みRead)。

参照: `er014_output/user_test_news_2ep_01/space_weapons/a2/audit/correction_01_secondary_asr_phrase_list.json`、`.../correction_01_raw_usage_log.jsonl`、`docs/pm/delegation_log/USER-TEST-NEWS-2EP-COMPLETION-01-CORRECTION-01.md`
